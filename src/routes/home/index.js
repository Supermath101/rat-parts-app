import { h } from 'preact';
import style from './style.css';

const Home = () => (
	<div class={style.home}>
		<h1>Home</h1>
		<p>{/* TODO: Add information about this website/app. */}</p>
	</div>
);

export default Home;
